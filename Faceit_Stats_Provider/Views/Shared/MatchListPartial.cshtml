@model List<MatchHistory.Item>

<div class="matchesList">
    @foreach (var match in Model)
    {
        <div class="match-item">
            <p>Match ID: @match.match_id</p>
            <!-- Add more match details here -->
        </div>
        var x = 1;
    }
</div>
